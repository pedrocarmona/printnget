- if @admin_company.errors.any?
  #error_explanation
    %h2= "#{pluralize(@admin_company.errors.count, "error")} prohibited this admin_company from being saved:"
    %ul
    - @admin_company.errors.full_messages.each do |msg|
      %li= msg

.actions
  = f.submit 'Save'
