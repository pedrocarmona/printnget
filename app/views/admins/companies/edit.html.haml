%h1 Editing admin_company
= form_for @admin_company, :url => admins_company_path(@admin_company),:method => :put do |f|
	= render 'form', :f => f

= link_to 'Show', admins_company_path(@admin_company)
\|
= link_to 'Back', admins_companies_path
